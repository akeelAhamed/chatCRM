# No meaningful functions to test in this file.
# The run_chatbot() function is a thin CLI wrapper that only orchestrates
# I/O (console printing and prompting) with no business logic, conditional
# branching, or transformations worth unit testing independently.